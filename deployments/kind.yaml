# a sample kind cluster to testing local development.
# > go install sigs.k8s.io/kind@latest
# > kind create cluster --config deployments/kind.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
- role: worker
